###  Frontend Documentation (`client/README.md`)

```markdown
# ⚛️ FinTrack Frontend

The frontend is a **React + TypeScript** application built with **Vite**. It features a "Creamy Cake" UI theme implemented with **Tailwind CSS**.

## 🎨 Features
* **Soft UI:** Custom Tailwind config for warm whites (`bg-stone-50`), soft shadows, and rounded aesthetics.
* **Component Architecture:**
    * `Dashboard.tsx`: Main overview with stats and quick-add form.
    * `BudgetPlanner.tsx`: Progress bars and budget limit settings.
    * `Layout.tsx`: Responsive sidebar and mobile navigation.
* **Icons:** Lucide React for consistent vector iconography.

## ⚙️ Installation & Setup

### 1. Install Node Modules
```bash
npm install